//comando para criar tabela
CREATE TABLE nome_da_tabela();

//comando para ler TODOS os dados da tabela
SELECT * FROM nome_da_tabela;

//comando para Gravar dados
INSERT INTO nome_da_tabela(Atributos_da_tabela) VALEUS (Valores_da_tabela)

//Mudar algo da Tabela
UPDATE usuarios
SET nome ='nome_usuario'
WHERE usuarios_id = numero_id

//Adicionar coluna
ALTER TABLE detalhes_usuarios
ADD COLUMN coluna_nova VARCHAR(15);

//Apagar uma coluna na tabela
ALTER TABLE detalhes_usuarios
DROP COLUMN nome_da_coluna

//Juntar duas tabelas
Left join //pega as informações da tabela da esquerda e o meio
Right join // pega as informações da tabela da direita e o meio
Full join// pega as informações da duas tabela e junta em uma
join  // pega as informações do meio da tabela

SELECT
   coluna1, coluna2 //pode colocar o quanto quiser. Isto faz para que apenas estas colunas apareçam quando executa
FROM
   Tabela1 // primeira tabela é a da esquerda
JOIN
   Tabela2 // segunda tabela é a da direita
ON
 é como se fosse um ligar, você liga tal coluna a outra 
 tabela.coluna_que_você_quiser = Tabela2.coluna_que_você_quiser